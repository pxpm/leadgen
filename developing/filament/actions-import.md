# Import Action (Filament v5)
**URL:** https://filamentphp.com/docs/5.x/actions/import
`ImportAction::make()->importer(ProductImporter::class)` in List page header. `php artisan make:filament-importer Product` defines columns mapping, `resolveRecord()` for create/update logic. CSV upload with column mapping.
