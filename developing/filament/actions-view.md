# View Action (Filament v5)
**URL:** https://filamentphp.com/docs/5.x/actions/view
`ViewAction::make()` opens record in View page or modal.
`->slideOver()` for sidebar view. `->infolist([TextEntry::make("name")])` for custom infolist.
