# Custom Fields (Filament v5)
**URL:** https://filamentphp.com/docs/5.x/forms/custom-fields
Extend `Field` class: `php artisan make:filament-form-field RangeSlider`. Override `$view` and add properties. Blade uses `$getStatePath()` for Livewire binding. `configureUsing()` for global defaults.
