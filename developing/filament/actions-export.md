# Export Action (Filament v5)
**URL:** https://filamentphp.com/docs/5.x/actions/export
`ExportAction::make()->exporter(ProductExporter::class)` in List page toolbar. `php artisan make:filament-exporter Product` defines export columns, `getFormats(): array` for CSV/XLSX.
